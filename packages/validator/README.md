# @vellum/validator

Static validator for vellum: detects unsupported features (e.g. `<canvas>` with text dependency) and warns on CSS that won't survive ratserization cleanly.

> **Status:** placeholder. Implementation begins in Phase 2 (see `PLAN.md`).
